import React from 'react';
import PropTypes from 'prop-types';

/**
 * Tela de registrar na aplicação.
 * @param props
 * @returns {JSX.Element}
 * @constructor
 */
const SignUp = props => (
	<h1>Registra na aplicação</h1>
);

SignUp.propTypes = {};

export default SignUp;
